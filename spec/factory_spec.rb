describe "Factory" do
  it "can construct a factory for instance objects out of a Frame" do
    skip
  end

	it "each instance object is a Frame"

	it "all the instance objects share the same public interface of messages"

	it "each instance object has the same data structure for representing state"

	it "each instance object has the same initial state"

	it "has a method (e.g. create) for creating instance objects"

	it "is there a factory for factories?"
end
